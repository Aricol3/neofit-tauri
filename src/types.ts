export interface NavIconProps {
  isActive: boolean;
}

export enum MACRO {
  CARBS = "carbs",
  FAT = "fat",
  PROTEIN = "protein"
}

export enum MEAL {
  BREAKFAST = "Breakfast",
  LUNCH = "Lunch",
  DINNER = "Dinner",
  SNACK = "Snack"
}